[project]
name = "battlebox-python-tools"
version = "0.1.0"
description = "Python helper tooling for the battlebox repository"
requires-python = ">=3.10"
dependencies = [
  "selenium>=4.31.0",
]

[tool.uv]
package = false
